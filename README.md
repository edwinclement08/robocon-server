P.S. From This point on (22-Aug-2021), this install is using SWAG, which auto renews the ssl certificate automatically

To start the app, run `docker-compose up -d`
To stop the app, run `docker-compose down`

